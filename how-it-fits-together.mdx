---
title: "How it fits together"
description: "How dozens of public repositories form a single ecosystem: infrastructure, configuration, Nix, AI development, and observability."
---

> Forty repositories sound like a lot. They're really five surfaces working as one stack.

## The full picture

```mermaid
%%{init: {'theme':'base','look':'handDrawn','themeVariables':{'primaryColor':'#4FB3A9','primaryTextColor':'#F4EFE6','primaryBorderColor':'#2F7E78','lineColor':'#E06B4A','secondaryColor':'#102937','tertiaryColor':'#0B1D2A','clusterBkg':'transparent','clusterBorder':'#4FB3A9','fontFamily':'Geist','fontSize':'15px'}}}%%
flowchart TB
  subgraph Inf["Infrastructure · Terraform"]
    direction LR
    tp(["terraform-proxmox"])
    ta(["terraform-aws"])
    tab(["terraform-aws-bedrock"])
    tasw(["terraform-aws-static-website"])
    tro(["terraform-runs-on"])
    tsa(["tf-splunk-aws"])
  end

  subgraph Cfg["Configuration · Ansible"]
    direction LR
    ap(["ansible-proxmox"])
    apa(["ansible-proxmox-apps"])
    as(["ansible-splunk"])
  end

  subgraph Nix["Nix Ecosystem"]
    direction LR
    nd(["nix-darwin"])
    na(["nix-ai"])
    nh(["nix-home"])
    ndv(["nix-devenv"])
  end

  subgraph AI["AI Development"]
    direction LR
    aai(["ai-assistant-instructions"])
    ccp(["claude-code-plugins"])
    ccr(["claude-code-routines"])
    aw(["ai-workflows"])
    rsi(["raycast-smart-issue"])
  end

  subgraph Obs["Observability"]
    direction LR
    vca(["VisiCore App"])
    vct(["VisiCore TA"])
    cco(["cc-edge-* packs"])
    csg(["cc-stream-* collectors"])
  end

  subgraph Tools["Tools & meta"]
    direction LR
    mlx(["mlx-benchmarks"])
    ok(["orbstack-kubernetes"])
    ss(["secrets-sync"])
    pt(["python-template"])
  end

  Inf -->|provision| Cfg
  Cfg -->|deploy| Obs
  Nix -->|tooling for| AI
  AI -->|telemetry from| Obs

  classDef cat fill:transparent,stroke:#4FB3A9,stroke-width:2px,color:#F4EFE6;
  class tp,ta,tab,tasw,tro,tsa,ap,apa,as,nd,na,nh,ndv,aai,ccp,ccr,aw,rsi,vca,vct,cco,csg,mlx,ok,ss,pt cat;

  linkStyle default stroke:#E06B4A,stroke-width:3px
```

## How each surface depends on the others

**Infrastructure → Configuration.** `terraform-proxmox` provisions VMs and LXC containers on the Proxmox cluster. `ansible-proxmox-apps` then configures Cribl, HAProxy, and the application layer on top.

**Configuration → Observability.** `ansible-splunk` deploys Splunk Enterprise. `ansible-proxmox-apps` deploys Cribl Edge and Stream. The Cribl packs (`cc-edge-*`, `cc-stream-*`) and Splunk apps (`VisiCore_*`) live on this infrastructure.

**Nix → AI Development.** `nix-ai` packages every AI coding tool — Claude, Gemini, Copilot, MLX, MCP servers. `nix-darwin` orchestrates the macOS system. `nix-home` handles the user environment. `nix-devenv` provides reusable per-project dev shells. Together they mean "rebuild my entire dev environment from a single `nix build`."

**AI Development → Observability.** Every AI coding interaction emits OTEL telemetry. The `cc-edge-*` Cribl packs collect it. Cribl Stream routes it. Splunk stores it. The `VisiCore_*` apps visualize it. End-to-end traceability for AI-assisted development — usage, cost, performance, all of it.

## Data flows worth knowing

For the actual data pipelines (logs, NetFlow, AI telemetry), see [Architecture · Data pipelines](/architecture/data-pipelines). For the AI development workflow (idea → PR), see [Architecture · AI pipeline](/architecture/ai-pipeline).

## Repository map

Each category has its own section in this site. Per-category pages link out to GitHub for each repo's `README.md`.

<CardGroup cols={3}>
  <Card title="Infrastructure" href="/infrastructure/overview">Terraform modules</Card>
  <Card title="Configuration" href="/configuration/overview">Ansible playbooks</Card>
  <Card title="Nix Ecosystem" href="/nix/overview">Reproducible everything</Card>
  <Card title="AI Development" href="/ai-development/overview">Multi-model AI pipeline</Card>
  <Card title="Observability" href="/observability/overview">OTEL → Cribl → Splunk</Card>
  <Card title="Tools" href="/tools/overview">Utilities and meta</Card>
</CardGroup>
