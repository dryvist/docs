name: Final PR Review

on:
  workflow_dispatch:
  pull_request_review:
    types: [submitted]

permissions:
  checks: read
  contents: read
  id-token: write
  issues: write
  pull-requests: write

jobs:
  review:
    uses: dryvist/ai-workflows/.github/workflows/final-pr-review.yml@main
    secrets: inherit
