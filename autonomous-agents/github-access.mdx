---
title: "GitHub access"
description: "How the independent OpenBao GitHub service engine issues short-lived GitHub App installation tokens without exposing the App private key."
page_type: explanation
---

<Note>
Status: **engine live, workstation cutover pending.** The engine was configured
and converged on 2026-07-17, with read, per-repo write, and admin tiers defined.
The workstation git/gh path and its PAT keychain tiers remain in place until the
credential-helper cutover passes live issuance, use, and revocation checks.
</Note>

> An agent gets an installation token that dies in an hour. It never sees the
> GitHub App private key that minted it.

The legacy model uses several PAT tiers in the macOS keychain. Shell functions
select a tier, and an interactive password gates the dangerous ones. That design
assumes a human at the keyboard.

The replacement is a dedicated GitHub App installed on the accounts where
automation runs. The
[`martinbaillie/vault-plugin-secrets-github` v2.3.2](https://github.com/martinbaillie/vault-plugin-secrets-github/releases/tag/v2.3.2)
plugin runs as an OpenBao service secrets engine at `github/`. OpenBao protects
the App private key and performs GitHub's installation-token exchange. Consumer
agents and runners cannot retrieve the private key from OpenBao. A privileged
provisioning controller briefly supplies it during bootstrap or rotation.

This is a service secrets engine, not GitHub authentication for OpenBao. Agents
still authenticate to OpenBao with their existing AppRole identity.

## Token flow

1. The agent authenticates to OpenBao as itself.
2. It requests a token at the tier its OpenBao identity allows.
3. OpenBao uses the App private key to ask GitHub for an installation token.
4. GitHub returns a token limited by the App installation and the tier's scope.
5. OpenBao returns the token with a lease. The token expires after one hour and
   can be revoked early through its lease.

## Three tiers

The engine exposes three access tiers. Each is a distinct OpenBao identity with
its own minting path, so the tier a caller holds is the whole privilege
boundary.

| Tier | Scope | How it mints | Gate |
| --- | --- | --- | --- |
| Read | All repositories of an account installation, read-only | A named read permission set that stores a read-only permission map | Ambient — reading is pre-authorized |
| Write | Exactly one named repository per request, at the App ceiling on that repo | The raw mint endpoint, with an OpenBao policy that requires and allowlists the repository parameter | Ambient for allowlisted repositories, plus a per-repository lease so two agents never hold the same repository |
| Admin | An account installation's full ceiling across every repository | A named full-automation permission set | Human-only: an interactive, single-use grant per elevation |

Read and admin tiers mint from named permission sets, whose stored scope the
request body cannot widen. The write tier is the one identity allowed to use the
raw mint endpoint, and its policy pins the request: the repository parameter is
required and drawn from a reviewed allowlist, while the parameters that would
name a whole installation or an arbitrary permission map are denied. So a write
token carries write access to a single named repository, not an account.

Adding a repository to the write allowlist is a reviewed configuration change,
not something a caller can do at request time. GitHub still enforces the outer
ceiling underneath all three tiers: an installation token can never exceed what
the App installation itself holds.

### The write lease

Concurrent agents must not both hold write on the same repository. Before it
mints a write token, the workstation helper takes a lease on that repository
inside OpenBao, using an atomic check-and-set so exactly one holder wins. The
lease carries a short deadman expiry, so a crashed holder frees it without a
cleanup process. The lease is cooperative — the sanctioned helper is the only
documented mint path — and it prevents two agents from owning one repository, not
repository corruption itself, which the remote already rejects on a stale push.

## Separation from AWS

The GitHub engine and the [AWS STS engine](/security/tools/openbao) are separate
logical engines inside one OpenBao security boundary:

| Boundary | AWS | GitHub |
| --- | --- | --- |
| Mount | `aws/` | `github/` |
| Root trust | AWS credential configured for the engine | GitHub App ID and private key |
| Output | STS credential lease | App installation-token lease |
| Authorization | OpenBao role plus AWS IAM | OpenBao permission-set path plus App installation |

They have separate mounts, configuration, policies, and leases. Normal API
access to one mount does not authorize access to the other. They still share the
OpenBao root and admin control plane, Raft storage boundary, hosts, plugin
runtime, and outbound HTTPS. A compromise of that shared boundary can affect
both engines.

## What it replaces

| Old access path | New mechanism |
| --- | --- |
| Routine read of any account repository | Ambient read tier — a read-only permission set |
| Automation that writes to a repository | Per-repository write tier — one allowlisted repository per request, behind a lease |
| Access outside the App permission ceiling | Human-only, interactive break-glass admin tier |

The service engine does not add a generic admin-verb broker. Routine reads and
per-repository writes are available to the agent within the reviewed allowlist.
An installation-wide token, or a repository outside the allowlist, stays on a
human-only, interactive break-glass path under
[Golden law #3](/security/golden-laws#3-human-approval-gates-every-potentially-destructive-action).
The password-gated human credential is never injected into automation.

## Residual risk, honestly

OpenBao becomes the path to the GitHub App private key. A caller that can use a
mint endpoint can mint tokens up to that tier's scope until its OpenBao access is
revoked. The controls are layered: the App installation ceiling, the tier's
scope (a read-only map, or a single allowlisted repository), path- and
parameter-scoped OpenBao policy, one-hour GitHub expiry, explicit lease
revocation, and OpenBao audit logs. Per-repository write keeps a leaked write
token's blast radius to one repository; the installation-wide admin tier stays
human-gated precisely because it is the broad one, which makes the shared
OpenBao boundary the target that matters.

The GitHub plugin is a third-party Apache-2.0 component. Its signed checksum
manifest, binary checksum, compatibility with the deployed OpenBao version,
token use, and revocation behavior must pass before the rollout is marked live.

## See also

<CardGroup cols={2}>
  <Card title="Secrets" icon="key" href="/autonomous-agents/secrets">
    Where GitHub installation tokens sit on the credential security ladder.
  </Card>
  <Card title="Roadmap" icon="map" href="/autonomous-agents/roadmap">
    How the service-engine rollout fits the wider agent program.
  </Card>
  <Card title="Golden laws" icon="scale-balanced" href="/security/golden-laws">
    The human-approval law that keeps destructive verbs out of the catalog.
  </Card>
  <Card title="Git transport" icon="lock" href="/conventions/git-transport">
    The SSH/HTTPS split the token tiers currently gate.
  </Card>
</CardGroup>
