---
title: "GitHub access"
description: "How the independent OpenBao GitHub service engine issues short-lived GitHub App installation tokens without exposing the App private key."
page_type: explanation
---

<Note>
Status: **rollout in progress.** The engine, fixed permission sets, and
path-scoped minting policy are code-defined. Existing PAT paths remain in place
until live issuance, use, and revocation checks pass.
</Note>

> An agent gets an installation token that dies in an hour. It never sees the
> GitHub App private key that minted it.

The legacy model uses several PAT tiers in the macOS keychain. Shell functions
select a tier, and an interactive password gates the dangerous ones. That design
assumes a human at the keyboard.

The replacement is a dedicated GitHub App installed on the accounts where
automation runs. The
[`martinbaillie/vault-plugin-secrets-github` v2.3.2](https://github.com/martinbaillie/vault-plugin-secrets-github/releases/tag/v2.3.2)
plugin runs as an OpenBao service secrets engine at `github/`. OpenBao protects
the App private key and performs GitHub's installation-token exchange. Consumer
agents and runners cannot retrieve the private key from OpenBao. A privileged
provisioning controller briefly supplies it during bootstrap or rotation.

This is a service secrets engine, not GitHub authentication for OpenBao. Agents
still authenticate to OpenBao with their existing AppRole identity.

## Token flow

1. The agent authenticates to OpenBao as itself.
2. It requests one named permission set from the `github/` mount.
3. OpenBao uses the App private key to ask GitHub for an installation token.
4. GitHub returns a token limited by the App installation and permission set.
5. OpenBao returns the token with a lease. The token expires after one hour and
   can be revoked early through its lease.

The engine has a fixed full-automation permission set for each account
installation. OpenBao policy grants callers `update` only on those named mint
endpoints. It does not grant them access to engine configuration or the
unrestricted `github/token` endpoint. This keeps installation choice and the
permission envelope in reviewed configuration instead of agent-supplied request
data.

The current permission sets deliberately omit repository and permission
restrictions, so each token receives the full repository and permission ceiling
of its App installation. That ceiling includes write access to contents,
workflows, secrets, hooks, pull requests, and organization-level automation
settings where the installation supports them. The narrow boundary is the
OpenBao path that callers may use, not the token's repository or operation
scope.

GitHub still enforces the outer ceiling: an installation token cannot exceed
the repositories or permissions granted to the App installation. An OpenBao
permission set can narrow that ceiling, but the current full-automation sets
narrow neither its repository list nor its permissions.

## Separation from AWS

The GitHub engine and the [AWS STS engine](/security/tools/openbao) are separate
logical engines inside one OpenBao security boundary:

| Boundary | AWS | GitHub |
| --- | --- | --- |
| Mount | `aws/` | `github/` |
| Root trust | AWS credential configured for the engine | GitHub App ID and private key |
| Output | STS credential lease | App installation-token lease |
| Authorization | OpenBao role plus AWS IAM | OpenBao permission-set path plus App installation |

They have separate mounts, configuration, policies, and leases. Normal API
access to one mount does not authorize access to the other. They still share the
OpenBao root and admin control plane, Raft storage boundary, hosts, plugin
runtime, and outbound HTTPS. A compromise of that shared boundary can affect
both engines.

## What it replaces

| Old access path | New mechanism |
| --- | --- | --- |
| Routine public and organization automation PATs | Named GitHub permission-set endpoint |
| Personal-account automation PAT | Separate named permission set for that installation |
| Access outside the App permission ceiling | Human-only, interactive break-glass path |

The service engine does not add a generic admin-verb broker. Operations inside
the reviewed full-automation ceiling are available to the agent, including the
configured organization-level writes. Anything outside that ceiling stays on a
human-only, interactive break-glass path under
[Golden law #3](/security/golden-laws#3-human-approval-gates-every-potentially-destructive-action).
The password-gated human credential is never injected into automation.

## Residual risk, honestly

OpenBao becomes the path to the GitHub App private key. A caller that can use a
permission-set endpoint can mint tokens up to that set's ceiling until its
OpenBao access is revoked. The controls are layered: the App installation
ceiling, fixed full-automation permission sets, path-scoped OpenBao policy,
one-hour GitHub expiry, explicit lease revocation, and OpenBao audit logs. The
broad permission sets make the shared OpenBao boundary a high-value target.

The GitHub plugin is a third-party Apache-2.0 component. Its signed checksum
manifest, binary checksum, compatibility with the deployed OpenBao version,
token use, and revocation behavior must pass before the rollout is marked live.

## See also

<CardGroup cols={2}>
  <Card title="Secrets" icon="key" href="/autonomous-agents/secrets">
    Where GitHub installation tokens sit on the credential security ladder.
  </Card>
  <Card title="Roadmap" icon="map" href="/autonomous-agents/roadmap">
    How the service-engine rollout fits the wider agent program.
  </Card>
  <Card title="Golden laws" icon="scale-balanced" href="/security/golden-laws">
    The human-approval law that keeps destructive verbs out of the catalog.
  </Card>
  <Card title="Git transport" icon="lock" href="/conventions/git-transport">
    The SSH/HTTPS split the token tiers currently gate.
  </Card>
</CardGroup>
